# Assets

This directory contains static assets for the AI Text Humanizer application.

## Contents

- `logo.svg` — Application logo (replace with your own branding)
- `images/` — Place image files (screenshots, icons, etc.) in this subdirectory

## Usage

Import assets in your components:

```tsx
import Logo from '@/assets/logo.svg'
```
