# Tests

This directory contains test files for the AI Text Humanizer application.

## Structure

- Place unit tests in `unit/`
- Place integration tests in `integration/`
- Place API route tests in `api/`

## Running Tests

```bash
npm run test        # Run all tests
npm run test:watch  # Run tests in watch mode
```
